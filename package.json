{
  "name": "px-bot",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "PX Bot — Professional Telegram Bot Admin Panel on Cloudflare Workers",
  "scripts": {
    "dev": "npm run build && wrangler dev --ip 0.0.0.0",
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19",
    "wrangler": "^4.129.0"
  },
  "engines": {
    "node": ">=20"
  }
}
